# gog

go to GitHub in go

## Installation

- Clone the repository
- Run `go build -o gog`
- Run `sudo mv gog /usr/local/bin/gog`

## Usage

```sh
go build -o gog
sudo mv gog /usr/local/bin
gog
```

```sh
gog -u {new_username}
```
